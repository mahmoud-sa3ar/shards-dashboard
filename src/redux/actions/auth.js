import { SET_LOGIN } from '../../utils/ActionTypes'


const setLogin = ()=>({


   /*  if (getUser.data !== null && getUser.status === 200) {
        const { Id, Type, Name } = getUser.data
        dispatch({
          type: INITIAL,
          data: { loggedIn: true, userData: { id: Id, type: Type, name: Name } },
        })
        localStorage.clear()
        localStorage.setItem('USER-LOGGED-IN', `id:${Id.toString()}, type: ${Type.toString()},name:${Name.toString()} `)
      } else {
        dispatch({
          type: INITIAL,
          data: { loggedIn: false, userData: {} },
        })
        localStorage.clear()
    } */


})
